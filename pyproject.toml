[project]
name = "sameface"
version = "0.1.0"
description = "Behavioral change detection for LLM endpoints that do not attest to you."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.scripts]
sameface = "sameface.canary:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
